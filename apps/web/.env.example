# Server-side only: target for Next.js rewrites proxy and server-side fetches.
# In production, set to the Render API origin (e.g., https://epde.onrender.com).
# Do NOT include /api/v1 — only the origin.
API_PROXY_TARGET="http://localhost:3001"
NODE_ENV="development"
