/** Recharts axis tick styling — SSoT for chart typography tokens. */
export const CHART_AXIS_TICK = {
  fontSize: 12,
  fill: 'var(--muted-foreground)',
} as const;

export const CHART_AXIS_TICK_SM = {
  fontSize: 11,
  fill: 'var(--muted-foreground)',
} as const;
